name = "leadpilot-ai"
main = "src/index.js"
compatibility_date = "2026-08-20"

[assets]
directory = "public"
binding = "ASSETS"

[vars]
APP_NAME = "LeadPilot AI"
PRICE_GBP = "49"

# D1 will be bound to this Worker after deployment from the Cloudflare dashboard.
# The Worker code is written to use the DB binding once it is attached.

[triggers]
crons = ["*/15 * * * *"]
